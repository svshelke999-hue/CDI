boto3>=1.34.0
botocore>=1.34.0
opensearch-py>=2.0.0
PyPDF2>=3.0.0
pdfplumber>=0.10.0  # Better PDF extraction (optional but recommended)
typing-extensions>=4.0.0
# OCR support for scanned/image-based PDFs (optional - install at least one)
pytesseract>=0.3.10  # Tesseract OCR wrapper (requires Tesseract OCR installed separately)
pdf2image>=1.16.0  # Convert PDF pages to images (requires poppler)
easyocr>=1.7.0  # Alternative OCR (self-contained, no external dependencies needed)
streamlit>=1.28.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
python-docx>=1.1.0
charset-normalizer>=3.0.0
